.. _mpi_compatibility:

MPI translation capabilities
=================================

**e4s-cl** is able to launch executables compiled with a different version than the one available on the system at runtime. This is achieved by different means depending on the requirements of the pair of libraries.

